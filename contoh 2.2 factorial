#include <stdio.h>

int factorial(int n);

int main() {
    int number = 5;
    int result = factorial(number);
    printf("factorial of %d: %d\n", number, result);
    return 0;
}

int factorial(int n) {
    if (n > 1)  {
        return n * factorial(n - 1);
    }
    else {
    return 1;
}
}
